/// <reference path='../models.ts' />

module Playbook.Models {
    export class GridSquare {
        public x: number;
        public y: number;
        constructor(x: number, y: number) {
            this.x = x;
            this.y = y;
        }
    }
}
