/// <reference path='../team.ts' />

module Team.Enums {
	export enum UnitTypes {
		Offense,
		Defense,
		SpecialTeams,
		Other,
		Mixed
	}

	export enum TeamTypes {
		Primary,
		Opponent,
		Other,
		Mixed
	}
}